| #   | name            | -                                                                             |
| --- | --------------- | ----------------------------------------------------------------------------- |
| 154 | SelectMany()    | 계층구조의 오브젝트의 시퀀스를 평탄화.<br> 배열속의 배열을 꺼내서 평탄화한다. |
| 155 | Concat()        | 두개의 시퀀스를 연결.                                                         |
| 156 | Zip()           | 두개의 시퀀스에 대응하는 요소를 tuple시퀀스로 변환.                           |
| 157 | SequenceEqual() | 두개의 시퀀스가 같은지 조사. IEquatabl<T>인터페이스 구현 필요.                |
| 158 | Range()         | 지정한 범위의 수치 컬렉션을 생성.                                             |
| 159 | Repeat()        | 지정한 값을 반복하는 시퀀스를 생성.                                           |
| 160 | Aggregate()     | 특수한 집합처리.                                                              |
| 161 | Where()         | 복수의 LINQ메소드를 연결.                                                     |
| 162 | GroupJoin()     | 외부join                                                                      |
| 163 |                 |                                                                               |
| 164 |                 |                                                                               |
| 165 |                 |                                                                               |
| 166 |                 |                                                                               |
| 167 |                 |                                                                               |
| 168 |                 |                                                                               |
| 169 |                 |                                                                               |
| 170 |                 |                                                                               |

```cs
// No. 154
var foods = array.SelectMany(x => x.FavoriteFood).Distinct();
var query = array.SelectMany(x => x.FavoriteFood, (o, food) => new { o.Name, Food = food });

// 155
var names = names1.Concat(names2);

// No. 156
var weeks = eweeks.Zip(jweeks).ToDictionary(x => x.Item1, x => x.Item2);

// No. 157
var equals = nums1.SequenceEqual(nums2);

// No. 158
var nums = Enumerable.Range(10, 8);

// No. 159
var seq = Enumerable.Repeat("Hello", 5).ToArray();

// No. 160
// 求める結果の初期値を「cherry」にする
// longestはその時点の求める結果
// nextには、要素が順に渡ってくる
var longestName = fruits.Aggregate("cherry", (longest, next) => next.Length > longest.Length ? next : longest);
var concatstr = fruits.Aggregate((t, f) => (f.Length == 5) ? $"{f}|{t}" : t);

// No. 161
var top2 = list
            .Where(x => x.Attempts >= 20)
            .Select(x => new
            {
                x.Name,
                Rate = (double)x.Success / x.Attempts
            })
            .OrderByDescending(x => x.Rate)
            .Take(2);

// No. 167
var query = persons
    .GroupJoin(sections,
        p => p.SectionCode,
        s => s.Code,
        (p, ps) => new { p, ps })
    .SelectMany(
        x => x.ps.DefaultIfEmpty(),
        (x, s) => new
            {
                Name = x.p.Name,
                Section = s == null ? "(None)" : s.SectionName
            }
    );

// No. 16
// No. 16
// No. 16
// No. 16
// No. 16
// No. 16
```
